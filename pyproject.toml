[project]
name = "hpyer"
repository = "https://github.com/Gelbpunkt/hpyer"

[build-system]
requires = ["maturin>=0.11,<0.12"]
build-backend = "maturin"

[tool.maturin]
manylinux = "Linux"
strip = true
